// Pessoa.ts
// Tipo usado para os dados de lista de pessoas atuais.
// O diretório app/legado foi mantido apenas como referência histórica.

export type Pessoa = {
  id: number;
  nome: string;
  profissao: string;
  sexo: string;
};
