// Person.ts
// Tipos usados para representar pessoas em diferentes partes da aplicação.
export type Pessoa = {
    id: number;
    nome: string;
    profissao: string;
    sexo: string;
};

export type Person = {
    id: number;
    name: string;
    profession: string;
    sexo: string;
};