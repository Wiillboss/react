// Person.ts (legado)
// Tipos legados usados para representar pessoas em inglês.
// A versão atual em português usa o tipo Pessoa no mesmo arquivo.
export type Pessoa = {
    id: number;
    nome: string;
    profissao: string;
    sexo: string;
};

export type Person = {
    id: number;
    name: string;
    profession: string;
    sexo: string;
};